flask>=2.0
flask-cors>=3.0
python-dotenv>=1.0
pyyaml>=6.0

# Voice pipeline (install with: pip install -r requirements.txt)
# Skip these if not using voice mode (VOICE_ENABLED=true)
pipecat-ai[silero]>=0.0.30
faster-whisper>=1.0.0
piper-tts>=1.2.0
pathvalidate>=3.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=12.0
rapidfuzz>=3.0
numpy>=1.24
# Install torch separately for CPU: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
